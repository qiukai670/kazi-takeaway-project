<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiukai.mapper.UserTokenMapper">

    <resultMap id="tokenResultMap" type="com.qiukai.entity.UserToken">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="token" column="token"/>
        <result property="expireTime" column="expire_time"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectByToken" resultMap="tokenResultMap">
        SELECT id, user_id, token, expire_time, create_time
        FROM user_token
        WHERE token = #{token} AND expire_time > NOW()
    </select>

    <insert id="insert" parameterType="com.qiukai.entity.UserToken" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_token (user_id, token, expire_time)
        VALUES (#{userId}, #{token}, #{expireTime})
    </insert>

    <delete id="deleteByUserId">
        DELETE FROM user_token WHERE user_id = #{userId}
    </delete>

    <delete id="deleteExpired">
        DELETE FROM user_token WHERE expire_time &lt;= NOW()
    </delete>

</mapper>
